services:
  - type: web
    name: frontend
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run dev
    envVars:
      - key: PORT
        value: 5173
